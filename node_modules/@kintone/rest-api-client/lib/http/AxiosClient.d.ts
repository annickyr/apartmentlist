import { HttpClient, ErrorResponse } from "./HttpClientInterface";
import FormData from "form-data";
declare type Headers = object;
declare type Params = {
    [key: string]: unknown;
};
declare type ErrorResponseHandler = (errorResponse: ErrorResponse) => void;
export declare class AxiosClient implements HttpClient {
    private baseUrl;
    private headers;
    private params;
    private errorResponseHandler;
    constructor({ baseUrl, headers, params, errorResponseHandler }: {
        baseUrl: string;
        headers: Headers;
        params: Params;
        errorResponseHandler: ErrorResponseHandler;
    });
    get(path: string, params: any): Promise<any>;
    getData(path: string, params: any): Promise<any>;
    post(path: string, params: any): Promise<any>;
    postData(path: string, formData: FormData): Promise<any>;
    put(path: string, params: any): Promise<any>;
    delete(path: string, params: any): Promise<any>;
    private handleError;
}
export {};
